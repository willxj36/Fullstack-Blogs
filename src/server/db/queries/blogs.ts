import { Query } from '../';

const all = () => {
    Query()
}

const one = (id: number) => {
    Query()
}

const put = (title: string, content: string, author: string, id: number) => {
    Query()
}

const post = (title: string, content: string, author: string) => {
    Query()
}

const deleter = (id: number) => {
    Query()
}

export default { all, one, put, post, deleter };